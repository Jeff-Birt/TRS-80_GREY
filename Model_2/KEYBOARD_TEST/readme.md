A simple keyboard test prgram for the Model 2 written in BASIC. The ASCII code for the key pressed is printed to the screen. 

For CTRL you can use CTRL + C to break out of the BASIC program. SHIFT keys can be test from command line.
